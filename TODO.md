# TODO

## Done

- [x] 搭建 Vite + React + TypeScript 前端项目
- [x] 搭建 Node.js + Express 后端 API
- [x] 增加 OpenAI 配置：`OPENAI_API_KEY`、`OPENAI_MODEL`、`PORT`
- [x] 默认模型配置为 `gpt-5.4`
- [x] 实现 `GET /api/config`
- [x] 实现 `POST /api/analyze`
- [x] 实现 `POST /api/storyboard`
- [x] 使用结构化输出校验理解档案和分镜结果
- [x] 增加模型不可用和 API Key 缺失的明确错误提示
- [x] 实现视觉风格选择：`2D`、`3D`、`3D高精度CG`、`仿真人`
- [x] 实现剧本风格选择：爽文、悬疑、言情、玄幻、都市、惊悚
- [x] 视觉风格和剧本风格支持自定义输入，并传入后端提示词
- [x] 视觉描述输入框常驻显示，支持在预设基础上直接改写为自定义风格
- [x] 实现语言选择：中文、英文
- [x] 实现阅读速率配置和实时重算
- [x] 实现目标分集数配置，并在分镜表、JSON、CSV 中记录分集编号
- [x] 支持多 `.txt` 文件导入，每个文件作为独立本地项目进入队列并可逐个处理
- [x] 增加本地历史记录面板，可查看、切换、删除旧项目并继续处理
- [x] 从 `.txt` 导入的项目按原文件名导出同名 Excel，并支持导出全部已完成项目
- [x] 增加页面内 API 配置面板，支持临时设置模型、API Key 和 Base URL
- [x] 增加 `POST /api/config`，支持本地运行时覆盖 API 配置
- [x] 增加 `GET /api/models`，应用 API 配置后自动读取可用模型列表
- [x] 将模型选择从 `datalist` 优化为真实下拉选择，并保留手动输入模式
- [x] API 配置保存到当前浏览器本地存储，刷新后自动恢复并检查连通状态
- [x] 开始创作取消阻塞确认弹窗，点击后立即显示理解/分镜生成状态
- [x] 英文模式下自动生成中文译文，便于对照查看
- [x] 分镜生成改为分段并发处理，降低长上下文漂移并提升生成效率
- [x] 增加 `/api/generate/stream` 分段流式生成接口，理解阶段推送进度、分镜阶段逐段推送镜头
- [x] 前端改为读取 NDJSON 流，分镜生成一段就追加一段，不再等待全部完成
- [x] 增加生成进度条和取消生成按钮，取消后保留已生成结果
- [x] 增加分镜失败后的手动“继续重试”能力，从失败段继续生成，保留已完成理解档案和已生成镜头
- [x] 分镜阶段连接错误失败时在错误栏和状态栏明确提示可从失败段继续重试
- [x] 取消生成时后端停止继续派发新分段，并向 OpenAI SDK 请求传递 abort signal
- [x] 流式分镜进度改为按已显示分段计数，避免并发乱序时进度快于表格
- [x] `done` 事件返回最终重排后的完整分镜，完成后前端替换临时分集编号，保证导出准确
- [x] 将长文分段从 8000 字降到约 2200 字，并优先按句子边界拆分
- [x] 为分段失败补充阶段和第几段错误提示，便于定位 8000 字以上文本失败点
- [x] 为 Responses API 请求补充 `instructions` 字段，兼容返回 `Instructions are required` 的网关
- [x] 将 Responses API 的 `input` 改为消息数组打包，兼容返回 `Input must be a list` 的网关
- [x] 增加 Chat Completions 兼容回退，处理第三方网关 Responses API 不完整导致的长文分段失败
- [x] 增加普通 Chat Completions 手动 JSON 解析兜底，兼容不支持 JSON Schema 的网关
- [x] 修正 `invalid character ... looking for beginning of value` 被误判为模型无权限的问题
- [x] 为网关非 JSON、500/502/503/504、连接中断等临时错误增加一次自动重试
- [x] 将临时网关错误重试升级为可配置指数退避，默认重试 3 次，并明确处理 `503 system cpu overloaded`
- [x] 将 `Connection error`、`network error`、`ECONNRESET/ECONNREFUSED` 等连接中断纳入临时错误重试和分镜续跑策略
- [x] 增加单次模型请求超时边界，避免长文本分镜请求无限等待或锁死
- [x] 将理解阶段、理解合并阶段和分镜阶段超时拆分配置，避免分镜 90 秒超时误伤理解合并
- [x] 理解合并改为串行执行，并在临时失败时拆小合并批次继续
- [x] 理解合并默认批量从 4 降到 2，并增加合并中心跳进度，避免长时间无 UI 反馈
- [x] 将理解合并改为本地确定性合并，不再调用模型，消除长篇合并阶段超时
- [x] 理解阶段连接错误失败后提供“重新尝试理解”入口，并保留当前文本、设置和历史记录
- [x] 增加分镜段自适应拆小重试，临时失败时按 `【分镜单元】` 拆分当前段继续生成
- [x] 分镜请求改用压缩理解档案摘要，减少每段上下文长度并降低超时概率
- [x] 将压缩理解档案升级为分段相关档案，按本段文本筛选人物、场景、事件、连续性提示，并补充前后段锚点
- [x] 增加 `JSSTORY_MOCK_AI` 本地 mock 生成模式和流式压测脚本，用于复现长文、503 和自动拆分重试
- [x] 增加分镜后台进度事件，避免并发分段乱序完成时 UI 看起来长时间不动
- [x] 增加生成中本地计时和后端 heartbeat，显示距离上次后端事件的等待时间
- [x] 将默认分段并发从 3 降到 2，降低第三方网关长文并发失败概率
- [x] 将非法并发配置的运行时兜底同步为 2，保持与默认并发文档一致
- [x] 收窄 Chat Completions 回退条件，避免真实模型不存在时额外消耗失败请求
- [x] 增加 `npm test` mock 测试，覆盖非 JSON 网关错误、模型不存在错误和默认并发兜底
- [x] 预计分段数量改为复用真实 `buildChunks` 结果，避免 UI 与后端切分不一致
- [x] 粘贴长文本时自动识别中英文，并同步切换阅读单位
- [x] 使用 `/Users/bey/Desktop/未命名.rtfd/TXT.rtf` 验证 52,827 字符长英文文本会被拆为 26 段
- [x] 限制单段最多生成 12 个镜头，减少结构化输出过长导致的网关失败
- [x] 分镜阶段增加短 `【分镜单元】` 切分，降低单镜头旁白过长导致视频生成不可用的风险
- [x] 分镜单元改为短句优先合并到相邻句，旁白朗读估算上限为 8 秒
- [x] 英文长句拆分增加过短尾段重平衡，避免句末单词被单独拆成镜头
- [x] 英文直接引语增加硬边界，避免上一句尾词和下一句对白粘连
- [x] 英文长对白拆分后保留引号形态，确保每段仍能继承说话人并识别为对白
- [x] 英文弯引号对白不再按内部 `! ? .` 过度拆分，引号闭合后的同句动作/反应可保持在同一分镜单元
- [x] 英文逗号/分号/冒号拆分只在引号外执行，避免 `"Lucy, come downstairs."` 被拆成两段
- [x] 英文接近镜头朗读上限且含逗号的叙述句按逗号保守拆分，减少长镜头但避免对白过碎
- [x] 分镜单元合并从页面阅读速率解耦，改用内部旁白语速估算，避免短句过度拆分
- [x] 分镜提示词补充人物/动作/视角/场景切换规则，要求插入第三方反应镜头或场景空镜保持连贯
- [x] 反应镜头和场景空镜支持无旁白，并按短过场时长估算
- [x] 规范 `durationSeconds` 语义为镜头画面时长，不再等同于旁白阅读时长
- [x] 长文理解结果改为每批 4 段递归合并，避免合并阶段再次形成超长上下文
- [x] 增强 `502 Upstream request failed` 的网关错误提示
- [x] 增强第三方网关排错提示，识别 Base URL 返回 HTML 的配置错误
- [x] 抽象并固化镜头提示词格式：时间段、镜头语法、画面细节、摄影机状态、声音设计、台词/旁白、导演批注
- [x] 固化旁白和角色对话引用格式：旁白：“内容”；角色名（情绪+发声方式）：“内容”；无内容时写 `无`
- [x] 增加直接引语识别规则：引号内发言即使缺少说话人也按角色对白处理，并用 `未指明说话者` 兜底
- [x] 固化提示词主体中文输出，同时保留旁白/对白引用内容的原文语言
- [x] 增加提示词安全画面化规则，要求高风险情节使用远景、遮挡、剪影、声画错位、旁人反应等非露骨表达
- [x] 增加视频生成字段清洗，只处理 `imageDescription`、`shotType`、`emotion` 和 `prompt`，保留原文、旁白和对白字段用于校对
- [x] 调整提示词清洗规则：只清洗画面/镜头/声音/导演批注行，`[台词/旁白]` 阅读行保留原文不做任何改写
- [x] 当同一 `[台词/旁白]` 行同时包含旁白和角色对白时，自动插入 `【停顿0.4s】`，并要求画面切到闭口反应或环境承接，避免口型穿帮
- [x] 将角色对白切到旁白的停顿升级为 `【停顿0.4s，角色名静默无发声】`，明确口型关闭要求
- [x] 扩展色情、血腥、暴力敏感表达的委婉改写：敏感接触改为安全部位和相对位置，血相关表达改为红色液体或暗色痕迹
- [x] 扩展镜头提示词语言：支持 `中景 -> 近景` 这类景别切换，并要求写明焦距变化和焦点转移
- [x] 将镜头语法固定标签调整为 `[镜头语法 / 视角 / 景别 / 焦距 / 焦点变化 // 运动或强度]`
- [x] 收紧景别切换策略：默认保持单一景别/焦距/焦点，只有有明确叙事原因时才切换
- [x] 补充人物动作表情规则：全身/半身包含表情细节、手部动作、身体姿态、视线方向；面部/近景/特写包含嘴角状态、眼部状态、面部状态、视线方向
- [x] 加入高级动画感审美指引，并明确不突破安全画面化和对白/旁白边界规则
- [x] 优化分镜表 UI：主字段行同步拉伸高度，提示词独立为更直观的导演工作台
- [x] 理解档案面板支持默认折叠滚动和展开查看，避免长档案撑开工作台
- [x] 实现中文/英文拆句、阅读单位统计、预计分段和总时长估算
- [x] 实现独立理解模块
- [x] 实现按画面归并的分镜生成提示
- [x] 实现分镜表内编辑
- [x] 实现 IndexedDB 本地自动保存和恢复
- [x] 实现 CSV 导出
- [x] 实现 Excel 导出，并按分集和镜头顺序排序
- [x] 导出提示词列按固定镜头标签格式化为单元格内多行，便于复制
- [x] Excel 导出使用单元格内换行标记，保证提示词列在 Excel 中显示为多行
- [x] 修正镜头编号显示和 CSV/Excel 导出口径，按每集重新从 1 编号，避免第 1 集镜头从 1 跳到 9
- [x] 收紧分镜提示词规则，禁止模型跨 `【分镜单元】` 合并原文字段
- [x] 收紧分镜提示词规则，要求拆分后的引号对白片段继续作为 `dialogueLines` 输出，并继承同一说话人
- [x] 增加分镜结果原文保真修复层：用原始 `【分镜单元】` 修复 `sourceText`、`narration`、`dialogueLines.text` 和 `[台词/旁白]` 行
- [x] 增加模型遗漏分镜单元的保底镜头补齐，避免长文生成后原文段落丢失
- [x] 增加过滤不在原始分镜单元内的模型台词，避免正常旁白中插入凭空生成对白
- [x] 原文保真修复层支持英文成对单引号对白，并避免把所有格撇号误判成对白
- [x] 取消场景证据后处理层：不再把模型返回的 `scene` 降级为 `未明确场景`
- [x] 取消场景继承和保守场景 prompt 重建，恢复以模型输出为主的具体画面策略
- [x] 过滤本段相关理解档案中的 0 分场景/人物/事件，减少无关场景被模型当作事实使用
- [x] 取消角色证据后处理层：不再过滤模型返回的 `characters`，不再移除视觉行里的泛化人物
- [x] 取消“基于原文的保守人物画面”prompt 重建，避免大量保守占位影响画面参考性
- [x] 增加最终质检器雏形，输出质量报告并在 notes 中追加质检摘要
- [x] 质检器阅读行一致性检查忽略 `【停顿0.4s】` 口型控制标记，避免误报正确停顿
- [x] 实现 JSON 导出
- [x] 完成基础工作台 UI
- [x] `npm run build` 通过
- [x] `npm test` 通过

## Next

- [ ] 增加更细的分段状态队列，显示每段等待中、生成中、已完成或失败
- [x] 增加失败重试能力，至少支持重试当前分段
- [ ] 增加超长镜头 UI 标记，提示哪些镜头可能超过视频生成平台时长限制
- [ ] 增加反应镜头/空镜的人工插入和一键补齐功能
- [ ] 增加暂停/继续机制，避免长篇生成中断后只能重来
- [ ] 增加 API 请求日志面板，便于排查模型错误、网关 502 和结构化输出错误
- [ ] 增加真实长篇小说测试集，验证分段、合并和成本表现
- [ ] 优化分集拆分算法，按阅读时长和剧情断点而不是单纯镜头数量均分
- [ ] 增加手动拆分/合并镜头功能
- [ ] 增加批量修改字段功能，例如批量改视觉风格、角色名、场景名
- [ ] 增加项目导入功能，支持读取导出的 JSON 恢复项目
- [ ] 增加 TXT 队列一键连续处理能力，并在失败时停在当前文件方便续跑
- [ ] 增加真正的 `.xlsx` 工作簿导出，支持列宽、筛选和多 Sheet
- [ ] 增加提示词模板管理，支持通用、即梦、可灵、Runway 等模板
- [ ] 增加可配置的敏感词和平台审核规则管理，支持按目标视频平台选择清洗强度
- [ ] 增加提示词格式预设选择，支持用户保存不同导演风格模板
- [ ] 增加角色一致性面板，集中编辑角色外貌、服装和连续性约束
- [ ] 增加场景库面板，集中维护地点、时间、氛围和视觉锚点
- [ ] 增加用量估算增强版，显示估算 tokens 和可能费用区间
- [ ] 为 `src/shared/text.ts` 增加单元测试
- [ ] 为 `src/shared/export.ts` 增加单元测试
- [ ] 增加语言规则测试，验证英文原文的旁白/对白在提示词引号内保持英文，提示词主体保持中文
- [ ] 为后端 API 增加 mock 模式，方便无 API Key 调试完整流程

## Decisions To Revisit

- [ ] 阅读速率 `0.95` 的精确定义：秒/字、秒/词，还是其他生产口径
- [ ] 默认模型是否长期保持 `gpt-5.4`，或改成账号可用性更稳定的配置
- [ ] 页面内 API 配置是否需要持久化到安全的本地后端配置文件
- [ ] 整本小说是否继续保持“一次点击生成”，还是改成章节级项目工作流
- [ ] 本地 IndexedDB 是否足够，还是需要 SQLite/文件项目库
- [ ] 是否需要引入登录和多项目管理
